# Prism

> A self-custodial multichain wallet that lets AI agents discover, authorize, and settle payments across every chain.

This README is rewritten in full at the end of the build. See
[`docs`](./docs) and per-package READMEs for current details.
